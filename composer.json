{
    "name": "jakubboucek/psync",
    "description": "A tool for automated deploy/download of PHP applications source code between local PC and dummy webhosting.",
    "license": "MIT",
    "type": "library",
    "authors": [
        {
            "name": "Jakub Bouček",
            "email": "pan@jakubboucek.cz"
        }
    ],
    "require": {
        "php": ">=8.4",
        "ext-curl": "*",
        "ext-json": "*",
        "ext-sodium": "*",
        "ext-zlib": "*",
        "symfony/console": "^7.1"
    },
    "require-dev": {
        "nette/tester": "^2.5",
        "php-parallel-lint/php-parallel-lint": "^1.4",
        "phpstan/phpstan": "^2.2",
        "phpstan/phpstan-strict-rules": "^2.0"
    },
    "suggest": {
        "ext-intl": "Unicode path normalization (NFC/NFD) so accented filenames match across macOS (NFD) and Linux (NFC) servers"
    },
    "autoload": {
        "psr-4": {
            "JakubBoucek\\Psync\\": "src/"
        }
    },
    "bin": [
        "bin/psync"
    ],
    "config": {
        "sort-packages": true
    },
    "scripts": {
        "check": [
            "@lint",
            "@phpstan",
            "@tester"
        ],
        "lint": "parallel-lint -e php,phpt src bin agent tests",
        "phpstan": "phpstan analyse --memory-limit=512M",
        "tester": "tester -C tests/Unit"
    }
}
